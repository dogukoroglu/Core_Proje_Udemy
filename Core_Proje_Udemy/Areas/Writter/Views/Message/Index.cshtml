@using EntityLayer.Concrete
@model List<WritterMessage>

@{
	ViewData["Title"] = "Index";
	Layout = "~/Views/Shared/_WritterLayout.cshtml";
}

@*@{
	string color = "";
}*@
<div class="col-lg-12 grid-margin stretch-card">
	<div class="card">
		<div class="card-body">
			<h4 class="card-title">Gelen Kutusu</h4>
			<div class="table-responsive">
				<table class="table">
					<thead>
						<tr>
							<th>#</th>
							<th>Konu</th>
							<th>Gönderen</th>
							<th>Detaylar</th>
						</tr>
					</thead>
					<tbody>
						@foreach (var item in Model)
						{
							@*if (item.WritterMessageID == "Yazı")
							{
								color = "badge badge-danger";
							}
							if (item.Status == "Proje")
							{
								color = "badge badge-warning";
							}
							if (item.Status == "Eğitim")
							{
								color = "badge badge-info";
							}
							if (item.Status == "Todo")
							{
								color = "badge badge-success";
							}*@
							<tr>
								<th>@item.WritterMessageID</th>
								<td>@item.Subject</td>@item
								<td>@(((DateTime)item.Date).ToString("dd-MMM-yyyy"))</td>
								<th><a href="/Writter/Default/AnnouncementDetails/@item.WritterMessageID" class="btn btn-primary">Mesajı Aç</a></th>
							</tr>
						}
					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>


